import React from "react";
import './App.scss'
import { Outlet, Link } from "react-router-dom";

export const App = () => {
    return (
        <main className="app">
          <header className="app__header"></header>
        </main>
    )
};